# Ten komponent nie definiuje żadnych symboli Kconfig.
# Wszystkie ustawienia przeniesiono do:
#   firmware/projects/demo_lcd_rgb/main/Kconfig.projbuild
#
# Menu pozostawiamy puste, by uniknąć duplikatów i ostrzeżeń.
