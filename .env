# ---------- ESP-IDF pin ----------
IDF_TAG=6.0
IDF_DIGEST=
IDF_IMAGE=esp32-idf:${IDF_TAG}-docs

# ---------- Domyślne parametry projektu ----------
PROJ=demo_lcd_rgb
TARGET=esp32c6
ESPBAUD=921600
MONBAUD=115200

# ---------- Rootless HOME w kontenerze ----------
# Stały mount względem katalogu repo (nie względem chwilowego $PWD).
DOCKER_HOME=/home/esp
DOCKER_HOME_MOUNT=${ROOT}/.idf-docker-home
